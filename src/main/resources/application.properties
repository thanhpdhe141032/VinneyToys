# Server configuration
server.port=8080

#PackagesToScan
entitymanager.packagesToScan=com.example.vinneytoy.models

# Database
dataSource.driverClassName=com.mysql.jdbc.Driver
dataSource.url=jdbc:mysql://localhost:3306/toys
dataSource.username=root
dataSource.password=123456789
datasource.initialize=true

spring.datasource.initialize=true

# MyBatis
mybatis.typeAliasesPackage=com.example.vinneytoy.models
mybatis.mapperLocations=classpath*:**/mappers/*.xml

# JWT
jwt.secret=hackhobomaycai
jwt.duration=604800